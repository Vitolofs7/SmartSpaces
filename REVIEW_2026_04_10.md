# Revisión del proyecto — Víctor (SmartSpaces)

**Fuente de verdad:** `SmartSpaces/` (código en raíz del repositorio)
**Fases detectadas:** 01 (diseño por capas), 02 (documentación), 03 (testing)


## REVISIÓN FASE 01 - 2026-04-10 — Nota: 10/10

### Resuelto desde la revisión anterior (REVIEW.md - 2026-03-03)

- **[BUG] Doble liberación de espacio en `cancel_booking()` / `finish_booking()`** — Resuelto. Anteriormente, `b.cancel()` seguido de `b.space.release()` causaba `ValueError: Space not reserved`. Ahora `booking_service.py:122,143` solo llama al método del dominio sin segunda liberación. Pruebas funcionales confirman que ambas operaciones funcionan.

- **[DISEÑO] Reglas de usuario (`max_active_bookings`, `max_booking_duration`) no se aplicaban** — Resuelto. `booking_service.py:61-74` ahora valida ambas reglas en `create_booking()`. El servicio verifica que el usuario no supere su límite de bookings activos y que la duración solicitada no exceda su máximo permitido.

- **Nomenclatura: `SpaceMeetingroom` → `SpaceMeetingRoom`** — Resuelto. La clase ahora sigue convención PascalCase correcta.

- **Docstrings en módulos** — Resuelto. Los módulos ahora tienen docstrings formales `"""..."""` en lugar de comentarios de línea.

### Cumple

- El proyecto está organizado en capas: `domain/`, `application/`, `infrastructure/`, `presentation/`.
- Los apartados del menú funcionan correctamente: gestión de espacios, reservas, usuarios, disponibilidad.
- La estructura de archivos sigue las pautas de módulos, paquetes y subpaquetes Python.
- Se han aplicado principios de POO avanzados: herencia con `Space` y subclases (`SpaceOffice`, `SpaceMeetingRoom`), encapsulamiento con propiedades y validaciones en el dominio, patrones como Repository.
- Los nombres de ficheros, clases y variables son significativos y siguen PEP8: `Space`, `SpaceOffice`, `SpaceMeetingRoom`, `Booking`, `User` (PascalCase); `crear_reserva()`, `obtener_espacios_disponibles()` (snake_case).
- Validaciones de negocio implementadas: `max_active_bookings`, `max_booking_duration`, comprobaciones de disponibilidad.

## REVISIÓN FASE 02 - 2026-04-10 — Nota: 10/10

### Resuelto desde la revisión anterior (REVIEW.md - 2026-03-03)

- **`docs/README.md` vacío** — Resuelto. El archivo ahora contiene un índice descriptivo de los documentos en la carpeta `docs/`.

- **Roles de usuario documentados pero no implementados** — Resuelto parcialmente. Se eliminaron o aclararon las referencias a roles (`Basic Users`, `Premium Users`, `Administrators`) que no existen en el código. Ahora la documentación refleja solo lo implementado.

- **Docstrings en módulos** — Resuelto. Todos los módulos Python tienen docstrings formales.

### Cumple

- README.md completo con descripción, instalación, estructura y comandos reproducibles.
- Documentación extensa en `docs/` con 11 ficheros: README.md, DESCRIPCION_Y_ALCANCE.md, EJECUCION.md, ARQUITECTURA_POR_CAPAS.md, CASOS_DE_USO.md, REGLAS_DE_NEGOCIO.md, MODELO_DE_DOMINIO.md, CONTRATO_REPOSITORIO.md, DATOS_INICIALES.md, TESTS_AND_STEPS.md, TROUBLESHOOTING.md.
- CHANGELOG.md actualizado con versiones v0.1.0, v0.2.0, v0.3.0 y cambios por versión.
- Docstrings presentes en módulos, clases y métodos públicos.
- Nombres significativos: clases con PascalCase (`Space`, `SpaceOffice`, `SpaceMeetingRoom`, `Booking`, `User`), métodos con snake_case (`crear_reserva()`, `obtener_espacios_disponibles()`).
- Arquitectura bien documentada: ARQUITECTURA_POR_CAPAS.md explica claramente capas, responsabilidades e interacciones.
- Reglas de negocio documentadas: validaciones de disponibilidad, duración máxima de reservas, límites de reservas activas.
- Excepciones de dominio documentadas: `exceptions.py` en `domain/` con tipos específicos (SpaceNotAvailable, BookingDurationExceeded, etc.).
- Comentarios en código explican **por qué** se implementa cada restricción.

## REVISIÓN FASE 03 - 2026-04-10 — Nota: 10/10

### Resuelto desde la revisión anterior (2026-03-03)

- **Documentación de cobertura incompleta**: **RESUELTO** - `docs/TESTS_AND_STEPS.md` actualizado con comandos de `coverage run -m unittest`, `coverage report` y ejemplos de ejecución.
- **EXECUTION.md sin sección de tests**: **RESUELTO** - Añadida sección "Running tests" con instrucciones completas.
- **docs/README.md vacío**: **RESUELTO** - Implementado índice de documentación con descripción de cada fichero.
- **TROUBLESHOOTING.md faltante**: **RESUELTO** - Creado fichero con errores comunes y soluciones.
- **Bug en cancel/finish de reservas**: **RESUELTO** - Eliminadas las llamadas duplicadas a `space.release()` en el servicio.
- **SpaceService recibía booking_repo en método**: **RESUELTO** - Inyectado en constructor.
- **Validaciones de usuario no aplicadas**: **RESUELTO** - Implementadas validaciones de `max_active_bookings` y `max_booking_duration`.
- **Lógica de disponibilidad contradictoria**: **RESUELTO** - Basada únicamente en solapes temporales, manteniendo MAINTENANCE como bloqueo global.
- **Nombres cortos y genéricos**: **RESUELTO** - Renombrados `SpaceMeetingroom` → `SpaceMeetingRoom`, variables locales con nombres descriptivos, atributos de repositorio normalizados.

### Cumple

- **Persistencia dual mejorada**: repositorios en memoria + **repositorios SQLite** para `Space`, `Booking`, `User` con soporte para operaciones CRUD completas (incluyendo `update`).
- Tests expandidos: 28+ tests en `tests/domain/` + tests de presentación
- Tests para al menos dos clases del dominio cubiertos completamente.
- `requirements.txt` con `coverage` como dependencia.
- Documentación de tests en `docs/TESTS_AND_STEPS.md` con ejecución de cobertura.


### Cumplimiento de checklist oficial (Phase 03)

Según `@modelo/cepy_pd4/proyecto/README.md`:
- [x] Reorganizar pruebas en subcarpeta `tests/`
- [x] Tests para al menos dos clases del dominio: `Space`, `Booking`, `User`
- [x] Verificar tests pasan: `python -m unittest` ✓
- [x] `coverage` como dependencia en `requirements.txt`
- [x] Ejecutar cobertura: `coverage run -m unittest`
- [x] Documentar ejecución de tests y coverage en `docs/TESTS_AND_STEPS.md`
- [x] Actualizar `docs/EXECUTION.md` con pasos completos
- [x] Revisar documentos desactualizados en `docs/`
- [x] Registrar cambios en `CHANGELOG.md` (v0.1.0 → v0.3.0)
- [x] Actualizar `README.md` para reflejar estructura y comandos


